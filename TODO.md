# 📋 Production To-Do — Intelligent Router

_Generated by the intelligent-router itself via `/complete` — routed to `gemini-3.5-flash` (cost $0.00097), demonstrating cost-optimized routing for a lightweight docs task._

---

## Real Model Pricing
- [ ] Implement an automated sync job to fetch daily pricing tables from major providers (OpenAI, Anthropic, Google).
- [ ] Configure local caching (Redis) for active pricing models with a 24-hour TTL fallback.
- [ ] Create a database schema and API endpoints to override and manually manage custom model pricing.

## OpenRouter Provider Integration
- [ ] Implement the OpenRouter API client, supporting custom headers (`HTTP-Referer`, `X-Title`).
- [ ] Map OpenRouter's specific error payloads to internal router exception types.
- [ ] Set up automatic fallback logic to secondary models/providers when OpenRouter returns 429 or 503.
- [ ] Parse and log OpenRouter's generation metadata for token tracking and exact billing.

## Auth & Security
- [ ] Implement API key generation, hashing (using bcrypt/Argon2), and revocation for routing clients.
- [ ] Configure OAuth2/JWT authentication for the FastAPI service and the usage dashboard.
- [ ] Add IP whitelisting and CORS configuration to the FastAPI middleware.
- [ ] Integrate a secrets manager (e.g., AWS Secrets Manager, HashiCorp Vault) for external provider LLM keys.
- [ ] Implement token bucket rate-limiting per API key.

## Persistence
- [ ] Spin up a production-ready PostgreSQL instance (or managed service) for logs, settings, and user data.
- [ ] Setup Alembic for database migrations and schema versioning.
- [ ] Implement connection pooling (SQLAlchemy + pgBouncer) to handle high-concurrency connections.
- [ ] Archive raw routing transaction logs older than 30 days to cold storage (e.g., S3).

## Observability
- [ ] Integrate Prometheus metrics for request latency, routing overhead, cost per request, and error rates.
- [ ] Set up structured JSON logging throughout the FastAPI service.
- [ ] Configure OpenTelemetry tracing for the request lifecycle (from prompt analysis to final provider response).
- [ ] Build a Grafana dashboard visualizing routing decisions, agent budget burn rates, and provider uptime.
- [ ] Set up Slack/PagerDuty alerts for high fallback rates, budget cap breaches, and 5xx errors.

## Load & Performance Testing
- [ ] Create a Locust or k6 test suite simulating production load (e.g., 100+ concurrent users).
- [ ] Profile prompt-analysis latency to ensure routing decision overhead remains under 50ms.
- [ ] Test system behavior under artificial provider outages to verify seamless fallback transitions.
- [ ] Validate database connection limits and Redis caching performance under peak stress.

## Production Deployment
- [ ] Write a production-ready Dockerfile optimizing for multi-stage builds and minimal image size.
- [ ] Set up a CI/CD pipeline (e.g., GitHub Actions) for linting, testing, building, and deploying.
- [ ] Deploy the FastAPI service and MCP server to a container orchestrator (e.g., Kubernetes, ECS).
- [ ] Configure an ingress controller (e.g., NGINX) with SSL termination and keep-alive connections.
